server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/seckill_dev?useSSL=false&serverTimezone=UTC
    username: root
    password: root
  redis:
    host: localhost
    port: 6379 